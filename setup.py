from setuptools import setup

setup(name='Trials',
      version='0.83',
      scripts=['__init__.py'],
      packages=['Trials],
      url='https://github.com/qAnupama/Trials.git',
      license='MIT',
      packages=['Trials']
                )
